{
  "name": "sabatomap-e2e",
  "private": true,
  "type": "module",
  "description": "地図の描画を実ブラウザで確かめる。本番の依存を汚さないよう package.json を分けてある",
  "scripts": {
    "test": "playwright test",
    "update": "playwright test --update-snapshots",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
